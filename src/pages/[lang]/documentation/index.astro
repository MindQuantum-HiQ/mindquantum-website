---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import DocumentationPage from "../../../components/pages/DocumentationPage.astro";
import { DOCUMENTATION_MESSAGES } from "../../../locales/documentation";
import { LANGS, DEFAULT_LANG, type Lang } from "../../../config/i18n";

export function getStaticPaths() {
  return LANGS.filter((l) => l !== DEFAULT_LANG).map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params as { lang?: string };
const safeLang: Lang = LANGS.includes((lang as Lang) || ("" as Lang))
  ? (lang as Lang)
  : DEFAULT_LANG;
const messages = DOCUMENTATION_MESSAGES[safeLang];
---
<BaseLayout lang={safeLang} title={`MindQuantum – ${messages.title}`} description={messages.metaDescription}>
  <DocumentationPage lang={safeLang} messages={messages} />
</BaseLayout>
